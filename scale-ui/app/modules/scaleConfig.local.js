(function () {
    'use strict';

    angular.module('scaleApp').constant('scaleConfigLocal', {
        urls: {
            prefixDev: '/api/', // dev
            prefixProd: '/api/',
        }

    });
})();
